defaults:
  - benchmark
  - scenario: inference
  # - launcher: process
  - backend: pytorch
  - _base_
  - _self_

name: cuda_pytorch_pythia_2b

# launcher:
#   device_isolation: true
#   device_isolation_action: warn

backend:
  device: cuda
  device_ids: 0
  no_weights: true
  torch_dtype: float16
  model: EleutherAI/pythia-2.8b
  model_kwargs:
    trust_remote_code: true
    use_cache: True

scenario:
  input_shapes:
    batch_size: 8
    sequence_length: 256
